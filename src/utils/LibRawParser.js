// LibRawParser.js
// Stub for parsing RAW metadata (white balance, etc.)
export function parseWhiteBalance(rawMetadata) {
  // Stub: extract temp/tint from metadata
  return { temp: 5500, tint: 0 };
}
