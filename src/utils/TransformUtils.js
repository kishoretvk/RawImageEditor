// TransformUtils.js
// Utility functions for rotate, flip, aspect lock, grid overlays
export function rotateImage(imageData, angle) {
  // Stub: rotate image by angle
  return imageData;
}
export function flipImage(imageData, horizontal = true) {
  // Stub: flip image horizontally/vertically
  return imageData;
}
