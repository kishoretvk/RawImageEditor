// OAuth2 scaffold for Google Drive, Dropbox, OneDrive
export async function signInWithProvider(provider) {
  // TODO: Implement OAuth2 popup and token handling
  alert(`Sign in with ${provider} (stub)`);
  return { provider, token: 'fake-token' };
}
